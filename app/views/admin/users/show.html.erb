<h1><%= @user.name %> の詳細</h1>

<p>
  <strong>名前:</strong> <%= @user.name %>
</p>

<p>
  <strong>メールアドレス:</strong> <%= @user.email %>
</p>

<p>
  <strong>作成日:</strong> <%= l(@user.created_at, format: :short) %>
</p>

<%= link_to '削除', admin_user_path, method: :delete, data: { confirm: '本当に削除しますか？' } %>
<%= link_to '戻る', admin_dashboards_path, class: 'btn btn-secondary' %>
